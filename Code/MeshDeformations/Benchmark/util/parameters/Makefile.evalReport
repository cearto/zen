NAME=report

#tryit: $(NAME).dvi
#	xdvi -s 4 $(NAME).dvi

${NAME}.pdf: $(NAME).ps
	ps2pdf $(NAME).ps $(NAME).pdf

${NAME}.ps: $(NAME).dvi
	dvips $(NAME).dvi -o $(NAME).ps

$(NAME).dvi: *.tex Makefile .
	latex $(NAME)

bib:
	latex $(NAME)
	bibtex $(NAME)
	latex $(NAME)
	latex $(NAME)
	make ${NAME}.pdf

clean:
	    rm -f $(NAME).pdf $(NAME).ps $(NAME).dvi *~




